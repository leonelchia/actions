name: build

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        timeout-minutes: 30
        steps:
            - name: checkout
              uses: actions/checkout@v5

            - name: welcome message
              run: |
                echo " This is just a CI practice"
                ls -ltra
                cat README.md 
            - name: Generate ASCII Artwork
              run: |
                chmod +x ascii-script.sh
                ./ascii-script.sh


    Test:
       needs: first_job
       uses: .github\workflows\reusable-test.yaml
             
             
    Deployment:
        needs:  Test
        runs-on: ubuntu-latest
        steps:
            - name: deploy
              run: |
               echo " nothing to deploy for now :)"         

            

 
